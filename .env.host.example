# [HOST MODE]
# Use this when running the app locally via `uv run uvicorn ...`
# You must ensure Redis is running and exposing port 6379 (e.g., docker run -p 6379:6379 ...)

REDIS_URL=redis://localhost:6379/0
REDIS_PORT=6379
